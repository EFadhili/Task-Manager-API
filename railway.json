{
  "build": {
    "builder": "NIXPACKS",
    "buildpacks": [
      "https://github.com/railwayapp/railpack-nixpacks"
    ]
  },
  "deploy": {
    "startCommand": "php artisan migrate --force && php artisan serve --host=0.0.0.0 --port=${PORT}",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300
  }
}
